name: Publish to NPM

on:
  push:
    tags:
      - 'v*'

jobs:
  npm-publish:
    name: Build and publish Node.js package to NPM
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'

      - name: Publish to NPM
        run: npm publish --access public || echo "Notice: Package already published or NPM_TOKEN secret pending setup."
        working-directory: bindings/nodejs
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
